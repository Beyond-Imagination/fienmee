{
    "name": "@fienmee/types",
    "version": "0.0.0",
    "description": "fienmee types",
    "main": "index.ts",
    "scripts": {
        "build": "rm -rf dist && swc src -d dist .",
        "test": "jest --passWithNoTests"
    },
    "private": true,
    "devDependencies": {
        "@babel/runtime": "^7.25.0"
    }
}
